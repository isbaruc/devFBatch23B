//Funcion clasica
/* let x = function suma(a, b) {
	return a + b;
}; */
function suma(a, b) {
	return a + b;
}

const suma = (a, b) => a + b; //se puede colocar el proceso sin llaves si solo es una línea de ejecución

const suma3 = (a, b) => {
	console.log(a + b);
	return;
};
